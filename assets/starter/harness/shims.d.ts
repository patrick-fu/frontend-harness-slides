// Ambient type declarations for the plain-JS harness modules.
// These files are intentionally authored in JavaScript so Playwright's node
// runtime can import them directly without an esbuild transpile step.
declare module '*.mjs';
